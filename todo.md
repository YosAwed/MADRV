# 実装タスク

- [x] GitHubリポジトリの構成とMADRVの実行・コンパイル経路を確認する。
- [x] ブラウザ内で利用できるMADRV／MML再生方式を設計する。
- [x] OPM（YM2151）、PCM（PDX）、GS互換MIDIの各再生経路と同期方式を設計する。
- [x] GS対応SoundFontまたはGS互換Web音源のライセンス、配布方式、ユーザー選択フローを確認する。
- [x] CORS許可済みのMDR／PDX URLとカタログURLを安全に取得するデータモデルを設計する。
- [x] MADRVファイル選択・ドラッグ＆ドロップのUIを実装する。
- [x] MMLエディタ、サンプル挿入、構文エラー表示を実装する。
- [x] リモートURL入力、MDR／PDXペア指定、取得中・CORSエラー表示を実装する。
- [x] GS音源の選択、ローカルSoundFont読み込み、音源未設定時のガイドを実装する。
- [x] Web MIDI APIで接続済みの外部MIDI出力を列挙し、内蔵SoundFontと切り替える。
- [x] ページ最下部にAwedのクレジットと、ソース準拠の「Based on MADRV MUSIC CONVERTER Version 1.10 (c)1991,92 Konoa」表記を表示する。
- [x] MMLのコンパイル結果を再生エンジンへ渡し、再生・停止・音量を統合する。
- [x] OPM・PCM・GS MIDIを同期再生し、ストリーム別の状態を表示する。
- [x] 再配布条件に適合するMXDRV／X68Sound相当のWebAssembly再生コアを選定し、ライセンス表記を整備する。
- [x] 提供されたMADRVSRC.LZHを展開し、原典のMDR再生、無限ループ、OPM／PCM／MIDI処理を対応表として整理する。
- [x] 提供されたMADRVFUNC.Lzhを展開し、Function Callの引数、状態遷移、MIDI／SysEx関連呼出しをWeb APIへ対応付ける。
- [x] 原典ソースの識別子・コメント・クレジットに準拠する実装注記をWebAssembly統合層へ追加する。
- [x] MDRのOPMイベントとPDXサンプル参照をWebAssembly再生コアへ渡し、完全再生する。
- [x] MDRのGS MIDIトラックからGS SysExを抽出し、内蔵音源と外部MIDI機器に適用する。
- [x] GS Reset、Part／Patch／Levelを操作できるSysEx送出パネルを実装する。
- [x] 外部GS MIDI出力機器を検出し、ノート・SysEx・All Notes Offの送出テストと状態表示を実装する。
- [x] 選択した外部MIDI機器へ、ノート、コントロール変更、All Notes Offを送出する。
- [x] CORS許可済みリモートMDR／PDXをブラウザ内で取得して再生する。
- [x] 無限ループを再生回数または最大書き出し秒数で明示的に上限管理する。
- [x] MMLの再生音をビジュアル付きMP4として書き出し、MP4非対応時は形式を明示して安全に代替する。
- [x] デスクトップ／モバイルでの動作とエラー処理を検証する。
- [x] Web MIDI出力機器の接続状態、送出メッセージ、診断テスト結果を表示・JSON保存する。
- [x] MDRのOPM・PCM・GS MIDIトラックを一覧化し、トラック別のミュート・ソロ・レベルを操作できるようにする。
- [x] CORS対応のリモートMDRカタログを取得・検索し、楽曲を選択して再生できるようにする。
- [x] カタログの楽曲をブラウザ内へお気に入り登録し、再訪時に復元する。
- [x] 動作確認済みのCORS対応MDRカタログをプリセットとして選択・読み込みできるようにする。
- [x] Google Drive共有リンクをMDR／PDX／カタログの直接取得URLへ正規化し、CORS可否を案内できるようにする。
- [x] CORS対応のSoundFont URLをブラウザ内シンセへ読み込み、ローカル選択と切り替えられるようにする。
- [x] MDR／PDX／SoundFont URLと再生設定をURLパラメータとして共有・復元できるセッションリンクを実装する。
- [x] Google Drive・Dropboxの公開共有ファイルを、許可ホスト・サイズ制限付きのサーバー側取得経路で読み込めるようにする。
- [x] モバイル／デスクトップでMML再生、リモート読込、共有リンク復元、SoundFont読込と代表的な取得失敗を確認して記録する。
- [x] Signal Deck診断カタログをプリセット選択し、Load catalog、項目表示、選曲、MDR読込までブラウザで確認する。
- [x] 375×812相当のモバイル幅で、MML再生・リモートMDR読込・共有リンク復元・SoundFont読込・無効URL失敗表示を実操作で確認して記録する。
- [x] 1280×720以上のデスクトップ幅で、同じ主要フローと失敗ケースを実操作で確認して記録する。
- [x] ローカルファイル選択でMDXがグレーアウトせず、MDR／MDX／PDXを正しく選べるよう修正・検証する。
- [x] MDX修正後のローカル選択回帰として、MDR単体・PDX追加・MDX単体の3パターンをブラウザ操作で確認して記録する。
- [x] MDX選択後に再生ボタンを押しても開始しない不具合を再現・修正し、再生開始から停止まで検証する。
- [x] MDXのタイトル・必要PDXファイル名・選択状態をローカル入力画面に詳細表示する。
- [x] 同時選択・追加済みのファイルから、MDXが必要とする同名PDXを自動検出して紐付ける。
- [x] MDX再生が無音となる原因をWebAssembly出力・PDX解決・Web Audio接続で診断し、実音声を確認して修正する。
- [x] MDX修正後に、ブラウザ上のWeb Audio出力が非ゼロPCMを生成していることを確実な計測方法で検証して記録する。
- [x] PCM／PDXの実出力中はENGINES表示をARMEDへ切り替え、停止後にAWAITへ戻すよう修正・検証する。
- [x] PCM未使用のMDXではPDXが選択済みでもAWAITを維持し、PCM使用MDXだけでARMEDとなる実発音判定を実装・検証する。
- [x] WebAssemblyでADPCMとPCM8の継続再生状態を返す真のPCM活動マスクを実装する。
- [x] PCM持続中はARMEDを維持し、発音終了後だけAWAITへ戻ることをブラウザ回帰検証する。
- [x] 再生デッキの再生位置マーカーを時間進行へ滑らかに追従させ、視認性を検証する。
- [x] 音量バーの役割を明示し、再生信号と混同しない表示へ整理する。
- [x] MDXヘッダーのPDX名に拡張子が含まれても、表示・案内・照合で`.PDX`を重複させないよう修正・検証する。
- [x] レトロPC音楽ファン向けのX投稿用MADRV Player告知ビジュアルを作成する。
- [x] 生成中のX投稿用告知ビジュアルが完成したことを確認し、完成画像を投稿用として提供する。
- [x] MDRトラックごとの発音状態を鍵盤表示で可視化する。
- [x] トラック別ミュートを明確なオン・オフ操作として表示・切替できるようにする。
- [x] 実ノートを含むMDR再生中に、OPM／MIDIトラック鍵盤が待機・点灯・停止後待機へ遷移することをChromiumで検証する。
- [x] 実ノートを含むOPMトラック付きMDRで、OPM鍵盤が待機・点灯・停止後待機へ遷移することをChromiumで検証する。
- [x] ローカルMDX／MDR再生時にもトラック鍵盤の状態表示が分かるよう、表示条件と配置を改善する。
- [x] 最新WebAssemblyプレーヤーで報告されたテンポ・ピッチの不正確さを旧版比較で再現・原因特定・修正する。
- [x] 原典ソースを転載・配布せず、仕様・動作上の知見のみを参照する独自実装方針をプロジェクトへ明記する。
- [x] 44.1 kHzなど非48 kHzのAudioContextでMDX／MDR再生クロックが同期することを診断・ブラウザ回帰で検証する。
- [x] 44.1 kHzのAudioContextでMDR再生もAudio clock表示とWebAssemblyクロックが同期することをブラウザ回帰で確認する。
- [x] 再生デッキへ曲のBPM推定値とAudioContext再生クロックの診断表示を追加し、再生中更新を検証する。
- [x] スマホ再生時の音声処理・描画更新・トラック表示の負荷を計測し、主な処理落ち要因を特定する。
- [x] モバイル幅では音声再生を優先する更新間引き・軽量表示を実装し、再生中のUI負荷を削減する。
- [x] スマホ相当Chromiumで長時間のMDX／MDR再生、鍵盤・診断・停止操作の性能回帰を検証する。
- [x] スマホ相当Chromiumで、30秒以上のMDX長時間再生中に鍵盤表示更新・BPM／Timer-B診断更新・停止操作成功を明示アサートする回帰を追加する。
- [x] スマホ相当Chromiumで、30秒以上のMDR長時間再生中に鍵盤表示更新・MXDRV／GS MIDI診断更新・停止操作成功を明示アサートする回帰を追加する。
- [x] スマホ相当Chromiumで、MDX再生中の推定BPM・Timer-B・軽量鍵盤更新と停止後のTimer-B復帰を明示アサートする。
- [x] スマホ相当Chromiumで、MDR再生中のGS MIDI同期差・MXDRV再生位置・軽量鍵盤更新と停止後のTimer-B復帰を明示アサートする。
- [x] 提供されたMEGALITH.MDR／PDXで冒頭停止を再現し、MDR構造・ループ・再生時間計測の原因を特定する。
- [x] MEGALITHを全曲再生できるようMDRの終了判定またはループ処理を修正する。
- [x] MEGALITHと既存MDX／MDRで再生開始・継続・停止の回帰を検証する。
- [x] MEGALITH.MDR／PDXを15秒以上継続再生し、明示停止後のUI復帰とTimer-BリセットをChromiumで検証する。
- [x] MDR全般で事前計測値に依存せず実行時の終了状態を使うよう再生終了判定を修正する。
- [x] MDR読込時に曲名、必要PDX、OPM／PCM／GS MIDIトラック構成、推定再生時間を表示する。
- [x] MDRメタデータパネルに推定再生時間を表示し、Chromium回帰で曲名・必要PDX・OPM／PCM／GS MIDI構成・推定再生時間を確認する。
- [x] Google Drive／Dropboxの共有URLから取得したデータをMDR・MDXとして正しく判定し、MDXをMDR再生経路へ渡さないよう修正する。
- [x] Google Drive共有URLの`view?usp=drive_link`などのURL断片ではなく、ファイル名またはMDX／MDRヘッダーの曲名を表示する。
- [x] Google Drive共有MDXとPDXの読込・MDX再生・表示名正規化をChromiumで回帰検証する。
- [x] 実際のGoogle Drive共有URLホストを使うChromium回帰を追加し、`publicStorage.fetchAsset`経由でMDX／PDXが取得されることを検証する。
- [x] Google Drive共有MDXで、ヘッダー曲名表示・MDX再生開始・停止までを実ホスト条件で確認する回帰を追加する。
- [x] 複数のMDRで長尺再生・自然終了または明示停止・MDRメタデータ表示をChromiumで回帰検証する。
- [x] HECT_GS2.MDRのOPM／PCMとGS MIDIのトラック別テンポ・イベント時刻を解析し、テンポ崩れの原因を特定する。
- [x] MDRのGS MIDIイベントをOPM／PCMと同じ再生クロックへ同期させ、トラック別テンポの不整合を修正する。
- [x] HECT_GS2とMEGALITHを含む複数MDRでテンポ同期と長尺再生をChromiumで回帰検証する。
- [x] MEGALITH.MDR／PDXを自然終了まで再生し、終了時刻・UI復帰・Timer-BリセットをChromiumで記録する。
- [x] HECT_GS2.MDRでOPM／PCMとGS MIDIのテンポ変化区間を比較し、共通クロック同期を客観的に回帰検証する。
- [x] HECT_GS2.MDRのテンポ変化区間ごとにGS MIDIイベント時刻とOPM／PCM再生位置を照合する自動回帰を追加する。
- [x] JESUS_SC.MDRの全トラック命令を解析し、全MIDI曲がOPM／PCMとして誤分類される原因を特定する。
- [x] MDRトラック分類を実行時のMIDIモードと抽出済みGS MIDIイベントに基づく方式へ修正する。
- [x] JESUS_SCの全MIDI表示と、HECT_GS2・MEGALITHの混在トラック表示・ミュート操作をChromiumで回帰検証する。
- [x] MDX／MDRの現在の有限ループ上限、自然終了、書き出し上限の処理を整理する。
- [x] 通常再生でMDX／MDRの無限ループ指定を明示停止まで継続する∞モードを実装する。
- [x] 有限ループ、∞通常再生、有限時間の書き出しが相互に干渉しないことをChromiumで回帰検証する。
- [x] ∞モードでもMML書き出しが1回・最大秒数へ有限化され、開始・完了・ファイル生成まで成立することをChromiumで検証する。
- [x] ∞モード導入後も有限ループのMDX／MDRが自然終了・停止通知を維持することをChromiumで再検証する。
- [x] ∞モード導入後の有限MDXで、自然終了時の再生ボタン復帰・終了通知・Timer-BリセットをChromiumで検証する。
- [x] ∞モード導入後の有限MDRで、自然終了時の再生ボタン復帰・終了通知・Timer-BリセットをChromiumで検証する。
- [x] 過去に報告された機能・不具合を要求一覧と現行実装・回帰スクリプトへ照合し、解決済み・未検証・環境依存へ分類する。
- [x] 外部MIDI実機、実Google Drive／Dropbox公開ファイル、実端末スマホなど、検証環境外でのみ成立する確認項目を明確化する。
- [x] 監査で特定した再現可能な残課題を修正・回帰検証し、公開状態へ保存する。
- [x] 監査で再現可能な残課題が無いことを、参照した回帰スクリプト・型検査・ユニットテスト・本番ビルドとともに監査記録へ明記する。
- [x] MDXのテンポ異常をDRA02.MDXなどで再現し、AudioContextサンプルレート・Timer-B・MXDRV再生位置・実時間の差を計測する。
- [x] MDX再生のクロックまたはレンダリング処理を修正し、曲のテンポ・音高を正しい速度へ戻す。
- [x] 修正後にMDXのテンポ・音高・再生位置をデスクトップとモバイル相当Chromiumで回帰検証する。
- [x] 修正後のデスクトップ相当Chromiumで、MDXの実時間対MXDRV再生位置比を再計測し、テンポ・再生位置の正常性を記録する。
- [x] 修正後にMDXのサンプルレートとTimer-Bから音高・再生クロックの整合を明示確認する回帰を追加する。
- [x] DRA02.MDXの44.1／48 kHz条件ごとに、既知Timer-B値と期待BPMの一致を明示アサートする。
- [x] MDX出力のサンプルクロック比を44.1／48 kHzで自動検証し、再生音のピッチ伸縮が発生しないことを確認する。
- [x] DRA02.MDXの実AudioContext出力をChromiumで44.1／48 kHz条件ごとに解析し、同一再生位置の支配周波数が一致することを確認する。
- [x] 44.1 kHz出力時もMDXコアを48 kHzで動かし、出力PCMを連続リサンプルしてテンポ・音高を維持する。
- [x] MDR再生の無音、再生位置異常、BPM診断異常を実データで再現し、音声ピーク・MXDRV位置・Timer-B・実時間を計測する。
- [x] MDRの48 kHzコアとAudioContext出力のサンプルクロック処理を修正し、OPM／PCM音声を復旧する。
- [x] MDRのMXDRV再生位置・Timer-B・推定BPMを実行中のコア状態から正しく表示する。
- [x] MEGALITH／HECT_GS2を含むMDRと既存MDXで、音声出力・再生位置・BPM・停止をChromium回帰検証する。
- [x] 修正後の変換器でHECT_GS2.MDRをChromium回帰し、GS MIDI同期差、MXDRV再生位置、Timer-B／BPM、停止復帰を確認する。
- [x] スマホ相当とPC相当でPCM音量スライダーの出力ゲインを計測し、PCM音量制御が反映されない経路を特定する。
- [x] MDX／PDX再生時の周期ノイズをモバイル幅・CPU負荷条件で再現し、音声バッファ不足、処理停止、PCMリサンプルのいずれかを切り分ける。
- [x] PCMゲイン制御とモバイル音声バッファ処理を修正し、ノイズ発生を抑制する。
- [x] スマホ相当ChromiumでPCM出力を波形解析し、ノイズ床、周期スパイク、PCM活動中の無音ドロップアウトを定量検証する。
- [x] 4倍CPUスロットルと通常CPUのPCM波形指標を比較し、バッファ期限超過・処理停止・リサンプル由来の異常がないことを確認する。
- [x] PC・モバイル相当のMDX／PDXでPCM音量操作と連続再生をChromium回帰検証する。
- [x] ローカルフォルダ選択でMDR／MDX／PDXを収集し、曲ごとのPDX紐付けを含むプレイリスト一覧を生成する。
- [x] リモートのCORS対応フォルダURLまたは安全なプレイリストマニフェストからMDR／MDX／PDX一覧を取得する。
- [x] プレイリストを画面へ表示し、任意曲の選択、現在曲、失敗曲の状態を操作可能にする。
- [x] プレイリスト再生時は各曲を2回ループして次曲へ自動送りし、最終曲の終了後に停止する。
- [x] ローカル・リモート両方の複数曲プレイリストで、2ループ自動送り・最終曲停止・中断操作をChromium回帰検証する。
- [x] 再生中に別曲を選択して開始した際、Playback positionが前曲の位置を引き継がず新曲の開始位置から正しく追従するよう修正・Chromium回帰検証する。
- [x] MDR内のGS MIDIトラックがOPM／PCMと異なる速度で再生される不具合を再現・修正し、複数MDRで同期をChromium回帰検証する。
- [x] 外部MIDI機器向けにGS MIDI送出の遅延補正設定を追加し、保存・即時適用・内蔵SoundFont非影響を回帰検証する。
- [x] BOMB.MDRでOPM／PCM／GS MIDIトラックのテンポが一致しない不具合を実データ解析し、共有タイムラインへ同期してChromium回帰検証する。
- [x] ローカルフォルダのプレイリストで一覧の曲を選択した際、表示上の曲と異なるインデックスが再生される不具合を修正し、複数曲でChromium回帰検証する。
- [x] MEGALITH.MDRを基準に長時間再生時のTimer-B・MXDRV位置・GS MIDI同期・音声コールバック期限を計測し、テンポ揺れともたつきを抑えるチューニングとChromium回帰検証を行う。
- [x] ファイル読み込み時に端末の再生余力を自動診断し、安定優先・標準・低遅延の推奨設定を提案・適用できるようにしてChromium回帰検証する。
- [x] OmegaGMGS2.sf2使用時に内蔵GS MIDIがOPM／PCMより先行して聞こえる現象をMEGALITH.MDRで測定し、SoundFont専用補正とChromium回帰検証を実装する。
- [x] MEGALITH.MDR再生中にPCMレベルを下げるとOPM／GS MIDIまで小さくなる不具合を修正し、PCM単独の独立制御をChromium回帰検証する。
- [x] Google Drive・Dropboxなどの公開共有URLからリモートSoundFontを取得できない不具合を修正し、共有URL・通常CORS URLでChromium回帰検証する。
- [x] セッション共有設定を短い共有IDで保存・復元できる短縮リンクを実装し、既存URLパラメータ形式との互換性をChromium回帰検証する。
- [x] SoundFontごとに内蔵GS MIDIの発音タイミング補正値を登録・保存・自動適用できるようにし、SoundFont切替のChromium回帰検証を行う。
- [x] SoundFont別発音補正の補正あり／補正なしを再生中に比較できるA/B試聴ボタンを実装し、保存済みプロファイル非破壊をChromium回帰検証する。
- [x] 停止操作後に内蔵SoundFontのMIDI演奏が残る不具合を修正し、予約済みイベント取消・全ノート停止・A/B試聴中の停止をChromium回帰検証する。
- [x] BOMB.MDRで再生中に内蔵SoundFont MIDIが無音かつ停止後に遅延発音する回帰を修正し、実MDRの再生・停止後無音をChromium回帰検証する。
- [x] SoundFont発音補正の入力欄で矢印キーによるカレット移動と負数の直接入力ができない不具合を修正し、Chromium回帰検証する。
- [x] 提供されたGoogle Drive共有SoundFont URL（1zgIyr_uP2_D_LSa0VhTl-FAInAV2bzaK）で読込失敗を再現・修正し、実リンクのブラウザロードを回帰検証する。
- [x] 公開環境でルートURLがHTTP 500となる静的配信先の不具合を修正し、公開サイト表示と共有SoundFontロードを回帰検証する。
- [x] 大容量SoundFontを通常URL・Google Drive／Dropbox共有URLから読み込む際に、取得済み容量・総容量・割合をプログレスバーで表示し、Chromium回帰検証する。
- [x] SoundFont進捗表示追加後に報告されたMDR再生不能を再現・原因特定・修正し、MDR再生と進捗表示をChromiumで回帰検証する。
- [x] 公開版で再発したMDR再生不能を実利用条件で再現し、開始処理・音声出力・停止まで根本修正して回帰検証する。
- [x] 提供されたGoogle Drive共有ファイル（1vNZaDBGuiwvzHPqmLyhNj00ewCm6ZdDK）を公開環境で再現し、形式・必要PDX・読込から再生開始までを修正・回帰検証する。
- [x] 共有セッションrVxAtutxでOPMがGS MIDIに対して累積遅延する現象を実測し、長時間クロック同期を修正・回帰検証する。
- [x] 共有セッションrVxAtutxで同期較正後に発生したテンポ過速を再現し、OPM本来の速度を維持した同期方式へ修正・回帰検証する。
- [x] 共有セッションrVxAtutxで開始約45秒後から発生するOPM遅延を、実MXDRV位置追従のGS MIDI送出で修正し、45秒以降を回帰検証する。
- [x] 共有セッションrVxAtutxで3分時点まで蓄積するOPM遅延を実測し、OPMテンポを変えずGS MIDIの長時間追従を根本修正・回帰検証する。
- [x] MacBook NeoのSafariでOUTRUNを4分以上再生した際に蓄積するOPM遅延を再現し、Safari互換の長時間音声時計追従へ修正・回帰検証する。SoundFont側の発音特性として扱い、ユーザー判断により追加調査を終了。
- [x] SoundFont発音タイミング補正の入力欄に、マウス操作で値を上下できる増減ボタンを追加し、直接入力・負数・A/B比較を回帰検証する。
- [x] BOMB.MDRの無限ループ再生で2周目のMIDI開始が大幅に遅れる不具合を再現し、ループ境界のMIDI送出を修正・回帰検証する。
- [x] 大容量SoundFont使用時にBOMB.MDRの無限ループ2周目でGS MIDIが遅れて再開する再発を修正し、実SoundFont波形で境界発音を回帰検証する。
- [x] デフォルトSoundFontでも発生するBOMB.MDR無限ループ2周目のGS MIDI遅延を、SoundFontサイズに依存しない根本原因として修正・回帰検証する。
- [x] 実機で残るBOMB.MDR無限ループ2周目のGS MIDIずれを、前周の未消音ノート・SoundFont発音状態を含めて修正し、複数周回で回帰検証する。
- [x] BOMB.MDRの境界GSノートリセットでOPM再開が不自然化し、GS MIDIが大幅遅延する回帰を撤回し、実OPMループ位置基準の再同期へ修正・回帰検証する。
- [x] AudioWorklet先行送出後も残るBOMB.MDRの2周目GS MIDIずれを、OPM実ループ原点とGS MIDI抽出時刻原点の照合に基づいて修正・回帰検証する。
- [x] PCM音量スライダーがOPMを含む全体音量へ影響する再発を修正し、独立制御が不安定ならX68000準拠のOPM/PCM一体音量操作へ整理・回帰検証する。
- [x] Default SoundFontボタンの押下だけで共有SoundFontを自動ロードし、音源入力をローカル初期表示・ローカル→リモート→MMLの順へ整理して回帰検証する。
- [x] Signal routingとPrivacy pathを上部の操作導線から外し、下部の補助情報として再配置してデスクトップ・モバイルで回帰検証する。
- [x] Source stationに最近使用したローカル音源名とリモートURLの履歴を追加し、選択・削除・再読込をブラウザ内保存で提供して回帰検証する。
- [x] 添付BOMB.MMLのループ・テンポ・GS MIDI初期化・トラック構造を解析し、MDRの2周目同期ずれとの関連を報告する。
- [x] BOMB.MDRのGS MIDI各トラックの実ループ起点を解析し、OPM実ループ位相に合わせて2周目以降を再スケジュールして回帰検証する。
- [x] BOMB.MDRの曲全体ループ窓による次周再開・停止が外部MIDI送出でも正しく動作することを検証し、必要な補正を実装する。
- [x] 全MDRの変換済み曲全体ループ窓を内蔵SoundFont・外部MIDI共通で利用し、曲別例外なしに周回再開・停止を同期させて複数MDRで回帰検証する。
- [x] SDI_ENDING.MDRを非ループ終曲の回帰対象に追加し、終盤のテンポ減速、通常終了、内蔵SoundFont・外部MIDIの停止を検証する。
- [x] PRIN_GS.MDRをSC-55・YM2151混在のループ曲として回帰対象に追加し、内蔵SoundFont・外部MIDIの曲全体ループ同期とGS初期化を検証する。
- [x] YS1_19GS.MDRを回帰対象に追加し、曲全体ループ窓、GS初期化、内蔵SoundFont・外部MIDIの送出と停止を検証する。
- [x] WB2_ENDGS.MDRを回帰対象に追加し、曲全体ループ窓または自然終了、GS初期化、内蔵SoundFont・外部MIDIの送出と停止を検証する。
- [x] U6_GENOCIDE.MDRを回帰対象に追加し、曲全体ループ窓、GS初期化、内蔵SoundFont・外部MIDIの送出と停止を検証する。
- [x] STC20_GS.MDRを回帰対象に追加し、曲全体ループ窓、GS初期化、内蔵SoundFont・外部MIDIの送出と停止を検証する。
- [x] OPM再生系をMIDI同期から独立して精査し、音程・テンポ・Timer-B・再生位置・曲全体ループ境界が複数MDRで正確であることを測定・回帰検証する。
- [x] PRIN_GS.MDRで確認したOPMコア周回周期と変換済みMIDI曲全体ループ窓の差を解析し、OPMとMIDIの周回位相ずれを防ぐ同期方法を検証・修正する。
- [x] MDX・MDRでOPMが二重に演奏される症状を再現し、OPM出力経路・AudioNode接続・停止処理を修正して単一出力を回帰検証する。
- [x] ループと無関係な不定時刻のOPMフランジングをMDX長時間再生で再現し、AudioContextコールバック遅延・バッファ連続性・レンダラー状態を計測して修正する。
- [x] 単一レンダラー化後のOPM音程が高く聞こえる報告を精査し、MDX・MDRの44.1/48 kHz出力で実周波数・Timer-B・再生位置を再測定して必要な補正を行う。
- [x] ブラウザ保存型プレイリストを追加し、ローカル／リモート曲の登録、並べ替え、削除、曲別ループ回数、前後移動、連続再生を既存再生フローへ統合する。
- [x] ソースコード、設定、テスト、公開ビルド、参照資産一覧を含むダウンロード可能なMADRV Playerバックアップを作成する。
- [ ] 再生必須のWASM、AudioWorklet、診断資産の実体を収集し、参照名だけでなくバイト列も含む完全バックアップを作成する。
